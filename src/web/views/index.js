createApp({
    data: {

    },
    methods: {
        init: async function () {
            let self = this;
            self.initListeners();
            // EventOp.pub(IndexEventsDef.checkToken);

        },
        initListeners: function () {
            let self = this;
        },
    }
});